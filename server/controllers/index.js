const { addArtist } = require('./addArtist');
const { getFavoriteArtists } = require('./getFavoriteArtists');
const { buildEventsData } = require('./buildEventsData');

module.exports = {
  addArtist,
  getFavoriteArtists,
  buildEventsData,
}